name: 'Draft a GitHub Release with ...'
description: '...'
author: '...'
branding:
  icon: filter
  color: red
runs:
  using: 'composite'
  steps:
    - run: |
        ruby action.rb
      shell: bash
