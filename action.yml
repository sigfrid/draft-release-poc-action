name: 'Draft a GitHub Release with ...'
description: '...'
author: '...'
branding:
  icon: filter
  color: red
runs:
  using: 'composite'
  steps:
    - run: |
        bundle exec ruby action.rb
      shell: bash
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
